import React from "react";
import ResponseStatusDetailPage from "./ResponseStatusDetailPage";

export default function ValidUrlsResponseStatusPage() {
  return React.createElement(ResponseStatusDetailPage, { slug: "valid-urls" });
}
