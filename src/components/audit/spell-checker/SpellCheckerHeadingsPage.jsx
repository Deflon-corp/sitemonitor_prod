import React from "react";
import SpellCheckerDetailPage from "./SpellCheckerDetailPage";

export default function SpellCheckerHeadingsPage() {
  return React.createElement(SpellCheckerDetailPage, { slug: "headings-spelling" });
}
